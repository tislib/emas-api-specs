get:
  tags:
    - Persons > Educations
  summary: Gets person education information
  description: >
    Gets person education information
  operationId: getPersonEducationsByPin
  parameters:
    - name: pin
      in: path
      description: Pin
      required: true
      schema:
        type: string
  responses:
    '200':
      description: Successful operation
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../components/education.yml'
